services:
  db:
    restart: unless-stopped
    ports:
      - 5435:5432

  flyway:
    restart: "no"

  app:
    restart: unless-stopped

  frontend:
    restart: unless-stopped
    ports:
      - 127.0.0.1:3003:3000
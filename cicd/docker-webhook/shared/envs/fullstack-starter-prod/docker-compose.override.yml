services:
  db:
    restart: unless-stopped
    ports:
      - 5437:5432

  flyway:
    restart: "no"

  backend:
    restart: unless-stopped

  frontend:
    restart: unless-stopped
    ports:
      - 127.0.0.1:3005:3000